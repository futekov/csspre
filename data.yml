Variables:
  basic:
    description: "Ability to declare variables and use them later"
    less:
      code: |
        @text-size: 10px;
        @font: Arial, Helvetica, sans-serif;
        @prim-color: #1bc2ff;

        body {
          color: @prim-color;
          font: 400 @text-size @font;
        }
    scss:
      code: |
        $text-size: 10px;
        $font: Arial, Helvetica, sans-serif;
        $prim-color: #1bc2ff;

        body {
          color: $prim-color;
          font: 400 $text-size $font;
        }
    sass:
      code: |
        $text-size: 10px
        $font: Arial, Helvetica, sans-serif
        $prim-color: #1bc2ff

        body
          color: $prim-color
          font: 400 $text-size $font
    stylus:
      code: |
        $text-size = 10px
        $font = Arial, Helvetica, sans-serif
        $prim-color = #1bc2ff

        body
          color: $prim-color
          font: 400 $text-size $font
    postcss:
      addons:
        - name: simple-vars
          url: https://github.com/postcss/postcss-simple-vars
      code: |
        $text-size: 10px;
        $font: Arial, Helvetica, sans-serif;
        $prim-color: #1bc2ff;

        body {
          color: $prim-color;
          font: 400 $text-size $font;
        }
    css:
      code: |
        body {
          color: #1bc2ff;
          font: 400 10px Arial, Helvetica, sans-serif;
        }
  default:
    description: "Default variables are overwritten by regular ones, no matter when they are declared"
    scss:
      code: |
        $var1: lorem !default;

        $var2: bar;
        $var2: default-text !default;

        .default-variables {
          foo: $var1 $var2;
        }
    sass:
      code: |
        $var1: lorem !default

        $var2: bar
        $var2: default-text !default

        .default-variables
          foo: $var1 $var2
    stylus:
      code: |
        $var1 ?= lorem

        $var2 = bar
        $var2 ?= default-text


        .default-variables
          foo: $var1 $var2
    css:
      code: |
        .default-variables {
          foo: lorem bar;
        }
  lazy:
    description: "Variable Hoisting, variables can be declared after being used"
    less:
      code: |
        .vars-at-bottom-work {
          foo: @var;
        }

        @var: bar;
    css:
      code: |
        .vars-at-bottom-work {
          foo: bar;
        }
  lookup:
    description: "Use the value of a previously declared property elsewhere"
    stylus:
      code: |
        .property-lookup
          height: 200
          margin-top: (@height/2)
    postcss:
      local: true
      addons:
        - name: property-lookup
          url: https://github.com/simonsmith/postcss-property-lookup
        - name: mathjs
          url: https://github.com/dtrussia/postcss-mathjs
      code: |
        .property-lookup {
          height: 200;
          margin-top: resolve(@height / 2);
        }
    css:
      code: |
        .property-lookup {
          height: 200;
          margin-top: 100;
        }
  scoped:
    description: "Restrict the scope of variables"
    less:
      code: |
        @var: 100;
        .has-local-var {
          @var: 200;
          foo: @var;
          bar: @var;
        }
        .takes-global-var {
          foo: @var;
        }
    scss:
      code: |
        $var: 100;
        .has-local-var {
          $var: 200;
          foo: $var;
          bar: $var;
        }
        .takes-global-var {
          foo: $var;
        }
    sass:
      code: |
        $var: 100
        .has-local-var
          $var: 200
          foo: $var
          bar: $var
        .takes-global-var
          foo: $var
    stylus:
      code: |
        $var = 100
        .has-local-var
          $var = 200
          foo: $var
          bar: $var
        .takes-global-var
          foo: $var
    stylus-alt:
      code: |
        $var = 100
        .has-local-var
          foo: $var = 200
          bar: $var
        .takes-global-var
          foo: $var
    postcss:
      local: true
      addons:
        - name: nested-vars
          url: https://github.com/jedmao/postcss-nested-vars
      code: |
        $var: 100;
        .has-local-var {
          $var: 200;
          foo: $var;
          bar: $var;
        }
        .takes-global-var {
          foo: $var;
        }
    css:
      code: |
        .has-local-var {
          foo: 200;
          bar: 200;
        }
        .takes-global-var {
          baz: 100;
        }

Variable Interpolation:
  basic:
    description: "Use variables as parts of selectors, properties, or values"
    less:
      code: |
        @var: color;

        .class-@{var} {
          @{var}: #7b3d66;
          background-@{var}: #7b3d66;
          content: @var;
        }
    scss:
      code: |
        $var: color;

        .class-#{$var} {
          #{$var}: #7b3d66;
          background-#{$var}: #7b3d66;
          content: $var;
        }
    sass:
      code: |
        $var: color

        .class-#{$var}
          #{$var}: #7b3d66
          background-#{$var}: #7b3d66
          content: $var
    stylus:
      code: |
        $var = color

        .class-{$var}
          {$var}: #7b3d66
          background-{$var}: #7b3d66
          content: $var
    postcss:
      addons:
        - name: simple-vars
          url: https://github.com/postcss/postcss-simple-vars
      code: |
        $var: color;

        .class-$var {
          $(var): #7b3d66;
          background-$(var): #7b3d66;
          content: $var;
        }
    css:
      code: |
        .class-color {
          color: #7b3d66;
          background-color: #7b3d66;
          content: color;
        }
  calc:
    description: "Use variables as inside `calc()` values"
    less:
      code: |
        @var: 5px;

        div {
          height: ~"calc(7em + @{var})";
        }
    scss:
      code: |
        $var: 5px;

        div {
          height: calc(7em + #{$var});
        }
    sass:
      code: |
        $var: 5px

        div
          height: calc(7em + #{$var})
    stylus:
      code: |
        $var = 5px

        div
          height: "calc(7em + %s)" % $var
    postcss:
      addons:
        - name: simple-vars
          url: https://github.com/postcss/postcss-simple-vars
      code: |
        $var: 5px;

        div {
          height: calc(7em + $var);
        }
    css:
      code: |
        div {
          height: calc(7em + 5px);
        }
  selectors:
    description: "Place a set of selectors into a variable and reuse it"
    less:
      code: |
        @headings: h1, .h1, h2, .h2, h3, .h3;

        @{headings} {
          font-family: "Great Custom Font", sans-serif;
        }
    scss:
      code: |
        $headings: "h1, .h1, h2, .h2, h3, .h3";

        #{$headings} {
          font-family: "Great Custom Font", sans-serif;
        }
    sass:
      code: |
        $headings: "h1, .h1, h2, .h2, h3, .h3"

        #{$headings}
          font-family: "Great Custom Font", sans-serif
    stylus:
      code: |
        $headings = h1, .h1, h2, .h2, h3, .h3

        {$headings}
          font-family: "Great Custom Font", sans-serif
    postcss:
      addons:
        - name: simple-vars
          url: https://github.com/postcss/postcss-simple-vars
      code: |
        $headings: h1, .h1, h2, .h2, h3, .h3;

        $headings {
          font-family: "Great Custom Font", sans-serif;
        }
    css:
      code: |
        h1, .h1, h2, .h2, h3, .h3 {
          font-family: "Great Custom Font", sans-serif;
        }
  selectors nested:
    description: "Nest a set of selectors with parent reference below another selector"
    scss:
      code: |
        $active: "&.is-active, &:hover";

        .selector {
          foo: bar;
          #{$active} {
            baz: qux;
          }
        }
    sass:
      code: |
        $active: "&.is-active, &:hover"

        .selector
          foo: bar
          #{$active}
            baz: qux
    stylus:
      code: |
        $active = "&.is-active, &:hover"

        .selector
          foo: bar
          {$active}
            baz: qux
    css:
      code: |
        .selector {
          foo: bar;
        }
        .selector.is-active,
        .selector:hover {
          baz: qux;
        }

Mixins:
  basic:
    description: "Support inclusion of mixins"
    less:
      code: |
        .simple-mixin() {
          foo: bar;
        }

        .include-mixin {
          .simple-mixin;
          baz: qux;
        }
    scss:
      code: |
        @mixin simple-mixin {
          foo: bar;
        }

        .include-mixin {
          @include simple-mixin;
          baz: qux;
        }
    sass:
      code: |
        =simple-mixin
          foo: bar

        .include-mixin 
          +simple-mixin
          baz: qux
    stylus:
      code: |
        simple-mixin()
          foo: bar

        .include-mixin 
          simple-mixin()
          baz: qux
    postcss:
      local: true
      addons:
        - name: mixins
          url: https://github.com/postcss/postcss-mixins
      code: |
        @define-mixin simple-mixin {
          foo: bar;
        }

        .include-mixin {
          @mixin simple-mixin;
          baz: qux;
        }
    css:
      code: |
        .include-mixin {
          foo: bar;
          baz: qux;
        }
  params:
    description: "Mixins that can receive parameters passed to them"
    less:
      code: |
        .bordered(@color; @thickness: 1px) {
          border: @thickness solid @color;
        }

        .mixins-params {
          .bordered(#cbf40d);
          .bordered(#d04fbc, 3px);
        }
    scss:
      code: |
        @mixin bordered($color, $thickness: 1px) {
          border: $thickness solid $color;
        }

        .mixins-params {
          @include bordered(#cbf40d);
          @include bordered(#d04fbc, 3px);
        }
    sass:
      code: |
        =bordered($color, $thickness: 1px)
          border: $thickness solid $color

        .mixins-params
          +bordered(#cbf40d)
          +bordered(#d04fbc, 3px)
    postcss:
      local: true
      addons:
        - name: mixins
          url: https://github.com/postcss/postcss-mixins
      code: |
        @define-mixin bordered $color, $thickness: 1px {
          border: $(thickness) solid $(color);
        }

        .mixins-params {
          @mixin bordered #cbf40d;
          @mixin bordered #d04fbc, 3px;
        }
    stylus:
      code: |
        bordered($color, $thickness = 1px)
          border: $thickness solid $color

        .mixins-params
          bordered(#cbf40d)
          bordered(#d04fbc, 3px)
    css:
      code: |
        .mixins-params {
          border: 1px solid #cbf40d;
          border: 3px solid #d04fbc;
        }
  params-named:
    description: "Mixins that have named placeholders for each parameter passed to them, allows to pass parameters in any order"
    less:
      code: |
        .size(@w, @h) {
          width: @w;
          height: @h;
        }

        .mixins-named-params {
          .size(@h: 10px, @w: 25px);
        }
    scss:
      code: |
        @mixin size($w, $h) {
          width: $w;
          height: $h;
        }

        .mixins-named-params {
          @include size($h: 10px, $w: 25px);
        }
    sass:
      code: |
        =size($w, $h)
          width: $w
          height: $h

        .mixins-named-params
          +size($h: 10px, $w: 25px)
    stylus:
      code: |
        size($w, $h)
          width: $w
          height: $h

        .mixins-named-params
          size($h: 10px, $w: 25px)
    css:
      code: |
        .mixins-named-params {
          width: 25px;
          height: 10px;
        }
  arguments:
    description: "Mixins with an unknown number of arguments passed to them"
    less:
      code: |
        .transition(...) {
          -webkit-transition: @arguments;
          -moz-transition: @arguments;
          transition: @arguments;
        }

        .mixins-arguments {
          .transition(opacity 200ms);
        }
    scss:
      code: |
        @mixin transition($args...) {
          -webkit-transition: $args;
          -moz-transition: $args;
          transition: $args;
        }

        .mixins-arguments {
          @include transition(opacity 200ms);
        }
    sass:
      code: |
        =transition($args...)
          -webkit-transition: $args
          -moz-transition: $args
          transition: $args

        .mixins-arguments
          +transition(opacity 200ms)
    stylus:
      code: |
        transition($args)
          -webkit-transition: $args
          -moz-transition: $args
          transition: $args

        .mixins-arguments
          transition(opacity 200ms)
    postcss:
      local: true
      addons:
        - name: mixins
          url: https://github.com/postcss/postcss-mixins
      code: |
        @define-mixin transition $args {
          -webkit-transition: $args;
          -moz-transition: $args;
          transition: $args;
        }

        .mixins-arguments {
          @mixin transition opacity 200ms;
        }
    css:
      code: |
        .mixins-arguments {
          -webkit-transition: opacity 200ms;
          -moz-transition: opacity 200ms;
          transition: opacity 200ms;
        }

Conditionals:
  basic:
    description: "If statement within a declaration"
    less:
      issues:
        - unusual syntax
      code: |
        .if-condition {
          foo: bar;
          & when (1 = 1) {
            baz: qux;
          }
        }
    scss:
      code: |
        .if-condition {
          foo: bar;
          @if 1 == 1 {
            baz: qux;
          }
        }
    sass:
      code: |
        .if-condition
          foo: bar
          @if 1 == 1
            baz: qux
    stylus:
      code: |
        .if-condition
          foo: bar
          if 1 == 1
            baz: qux
    postcss:
      addons:
        - name: conditionals
          url: https://github.com/andyjansson/postcss-conditionals
      code: |
        .if-condition {
          foo: bar;
          @if 1 == 1 {
            baz: qux;
          }
        }
    css:
      code: |
        .if-condition {
          foo: bar;
          baz: qux;
        }
  ternary:
    description: "Ternary operator `x > 0 ? true : false`"
    scss:
      issues:
        - unusual syntax
      code: |
        .if-ternary {
          one-equals-one: if(1 == 1, true, false);
        }
    sass:
      issues:
        - unusual syntax
      code: |
        .if-ternary
          one-equals-one: if(1 == 1, true, false)
    stylus:
      code: |
        .if-ternary
          one-equals-one: 1 == 1 ? true : false
    css:
      code: |
        .if-ternary {
          one-equals-one: true;
        }
  property:
    description: "Interpolate `if` statements inside property names"
    scss:
      code: |
        .if-within-property {
          #{if(1 == 1, "property", "false")}: bar;
        }
    sass:
      code: |
        .if-within-property
          #{if(1 == 1, "property", "false")}: bar
    stylus:
      code: |
        .if-within-property
          {1 == 1 ? "property" : "false"}: bar
    css:
      code: |
        .if-within-property {
          property: bar;
        }
  selector:
    description: "Interpolate `if` statements inside selectors"
    scss:
      code: |
        .if-within-selector--#{if(1 == 1, "true", "false")} {
          foo: bar;
        }
    sass:
      code: |
        .if-within-selector--#{if(1 == 1, "true", "false")}
          foo: bar
    stylus:
      issues:
        - requires additional variable
      code: |
        $if-result = 1 == 1 ? "true" : "false"

        .if-within-selector--{$if-result}
          foo: bar
    css:
      code: |
        .if-within-selector--true {
          foo: bar;
        }

Rounding Numbers:
  round floor ceil:
    description: "Use of Round, Floor, and Ceil functions to convert a decimal to an integer"
    less:
      code: |
        .round-number {
          fn-round: round(1.618em);
          fn-floor: floor(1.618em);
          fn-ceil: ceil(1.618em);
        }
    scss:
      code: |
        .round-number {
          fn-round: round(1.618em);
          fn-floor: floor(1.618em);
          fn-ceil: ceil(1.618em);
        }
    sass:
      code: |
        .round-number
          fn-round: round(1.618em)
          fn-floor: floor(1.618em)
          fn-ceil: ceil(1.618em)
    stylus:
      code: |
        .round-number
          fn-round: round(1.618em)
          fn-floor: floor(1.618em)
          fn-ceil: ceil(1.618em)
    postcss:
      local: true
      addons:
        - name: mathjs
          url: https://github.com/dtrussia/postcss-mathjs
      code: |
        .round-number {
          fn-round: resolve(round(1.618))em;
          fn-floor: resolve(floor(1.618))em;
          fn-ceil: resolve(ceil(1.618))em;
        }
    css:
      code: |
        .round-number {
          fn-round: 2em;
          fn-floor: 1em;
          fn-ceil: 2em;
        }
  custom precision:
    description: "Customize the precision for a particular decimal number"
    less:
      code: |
        .precision-round {
          @number: 3.14159265359px;
          border-radius: round(@number, 1);
          border-radius: round(@number, 2);
          border-radius: round(@number, 3);
          border-radius: round(@number, 4);
        }
    scss:
      issues:
        - requires custom function
      code: |
        @function pow($x, $n) {
          $ret: 1;

          @if $n >= 0 {
            @for $i from 1 through $n {
              $ret: $ret * $x;
            }
          } @else {
            @for $i from $n to 0 {
              $ret: $ret / $x;
            }
          }
          @return $ret;
        }

        @function precision-round($float, $digits) {
          $sass-precision: 5;

          @if $digits > $sass-precision {
            @warn "Sass sets default precision to #{$sass-precision} digits, and there is no way to change that for now."
            + "The returned number will have #{$sass-precision} digits, even if you asked for `#{$digits}`."
            + "See github.com/sass/sass/issues/1122 for further informations.";
          }

          $pow: pow(10, $digits);

          @return round($float * $pow) / $pow;
        }

        .precision-round {
          $number: 3.14159265359px;
          border-radius: precision-round($number, 1);
          border-radius: precision-round($number, 2);
          border-radius: precision-round($number, 3);
          border-radius: precision-round($number, 4);
        }
    sass:
      issues:
        - requires custom function
      code: |
        @function pow($x, $n)
          $ret: 1

          @if $n >= 0
            @for $i from 1 through $n
              $ret: $ret * $x
          @else
            @for $i from $n to 0
              $ret: $ret / $x

          @return $ret

        @function precision-round($float, $digits)
          $sass-precision: 5

          @if $digits > $sass-precision
            @warn "Sass sets default precision to #{$sass-precision} digits, and there is no way to change that for now." + "The returned number will have #{$sass-precision} digits, even if you asked for `#{$digits}`." + "See github.com/sass/sass/issues/1122 for further informations."

          $pow: pow(10, $digits)

          @return round($float * $pow) / $pow

        .precision-round
          $number: 3.14159265359px
          border-radius: precision-round($number, 1)
          border-radius: precision-round($number, 2)
          border-radius: precision-round($number, 3)
          border-radius: precision-round($number, 4)
    stylus:
      code: |
        .precision-round
          $number = 3.14159265359px
          border-radius: round($number, 1)
          border-radius: round($number, 2)
          border-radius: round($number, 3)
          border-radius: round($number, 4)
    postcss:
      local: true
      addons:
        - name: mathjs
          url: https://github.com/dtrussia/postcss-mathjs
        - name: simple-vars
          url: https://github.com/postcss/postcss-simple-vars
      code: |
        .precision-round {
          $number: 3.14159265359;
          border-radius: resolve(round($number, 1))px;
          border-radius: resolve(round($number, 2))px;
          border-radius: resolve(round($number, 3))px;
          border-radius: resolve(round($number, 4))px;
        }
    css:
      code: |
        .precision-round {
          border-radius: 3.1px;
          border-radius: 3.14px;
          border-radius: 3.142px;
          border-radius: 3.1416px;
        }
  custom global precision:
    description: "Customize the global precision for calculations resulting in decimal numbers"
    scss:
      code: |
    sass:
      code: |

Random Number:
  basic:
    description: "Generate a random number with upper and lower limits"
    less:
      issues:
        - requires custom function
      code: |
        .make-random(@min, @max) { 
          @rnd: floor(`Math.random() * @{max} + @{min}`);
        }

        .random-integer {
          .make-random(1, 100);
          foo: @rnd;
        }
    scss:
      version: 3.3.0
      code: |
        .random-integer {
          foo: random(100);
        }
    sass:
      version: 3.3.0
      code: |
        .random-integer
          foo: random(100)
    stylus:
      issues:
        - requires custom function
      code: |
        random(min, max)
          return floor( math(0, "random") * max + min )

        .random-integer
          foo: random(1, 100)
    postcss:
      local: true
      addons:
        - name: mathjs
          url: https://github.com/dtrussia/postcss-mathjs
      code: |
        .random-integer {
          foo: resolve(floor(random() * 100 + 1));
        }
    css:
      code: |
        .random-integer {
          foo: 56;
        }
  cache:
    description: "Append the random number to a resource as a query string to invalidate the cache"
    less:
      issues:
        - requires custom function
      code: |
        .make-random(@min, @max) { 
          @rnd: floor(`Math.random() * @{max} + @{min}`);
        }

        .random-number-cache-invalidation {
          .make-random(1000, 9999);
          background-image: src("x.jpg?v=@{rnd}");
        }
    scss:
      version: 3.3.0
      code: |
        .random-number-cache-invalidation {
          background-image: src("x.jpg?v=#{random(9000) + 999}");
        }
    sass:
      version: 3.3.0
      code: |
        .random-number-cache-invalidation
          background-image: src("x.jpg?v=#{random(9000)+999}")
    stylus:
      issues:
        - requires custom function
      code: |
        random(min, max)
          return floor( math(0, "random") * max + min )

        .random-number-cache-invalidation
          background-image: src("x.jpg?v=" + random(1000, 9999))
    postcss:
      local: true
      addons:
        - name: mathjs
          url: https://github.com/dtrussia/postcss-mathjs
      code: |
        .random-number-cache-invalidation {
          background-image: src(x.jpg?v=resolve(floor(random() * 9999 + 1000)));
        }
    css:
      code: |
        .random-number-cache-invalidation {
          background-image: src("x.jpg?v=5656");
        }
  color:
    description: "Generate different colors with a random Hue or other color components"
    less:
      issues:
        - requires custom function
      code: |
        .make-random(@min, @max) { 
          @rnd: floor(`Math.random() * @{max} + @{min}`);
        }

        .random-number-color {
          .make-random(0, 360);
          color: hsl(@rnd, 50%, 50%);
        }
    scss:
      version: 3.3.0
      code: |
        .random-number-color {
          color: hsl(random(361) - 1, 50%, 50%);
        }
    sass:
      version: 3.3.0
      code: |
        .random-number-color
          color: hsl(random(361)-1, 50%, 50%)
    stylus:
      issues:
        - requires custom function
      code: |
        random(min, max)
          return floor( math(0, "random") * max + min )
    postcss:
      local: true
      addons:
        - name: mathjs
          url: https://github.com/dtrussia/postcss-mathjs
      code: |
        .random-number-color {
          color: hsl(resolve(floor(random() * 360 + 0)), 50%, 50%);
        }
    css:
      code: |
        .random-number-color {
          color: #4abf40;
        }

Parent Reference:
  append:
    description: "Append selector to the parent when nesting `&:hover`, `&.active`"
    less:
      code: |
        .selector-append {
          foo: bar;
          &:hover {
            baz: qux;
          }
        }
    scss:
      code: |
        .selector-append {
          foo: bar;
          &:hover {
            baz: qux;
          }
        }
    sass:
      code: |
        .selector-append
          foo: bar
          &:hover
            baz: qux
    stylus:
      code: |
        .selector-append
          foo: bar
          &:hover
            baz: qux
    postcss:
      addons:
        - name: nested
          url: https://github.com/postcss/postcss-nested
      code: |
        .selector-append {
          foo: bar;
          &:hover {
            baz: qux;
          }
        }
    css:
      code: |
        .selector-append {
          foo: bar;
        }
        .selector-append:hover {
          baz: qux;
        }
  extend name:
    description: "Extend the name of the parent selector `&--modifier-class`"
    less:
      code: |
        .extend-name {
          &-one {
            foo: bar;
          }
          &two {
            baz: qux
          }
        }
    scss:
      version: 3.3.0
      code: |
        .extend-name {
          &-one {
            foo: bar;
          }
          &two {
            baz: qux;
          }
        }
    sass:
      version: 3.3.0
      code: |
        .extend-name
          &-one
            foo: bar
          &two
            baz: qux
    stylus:
      code: |
        .extend-name
          &-one
            foo: bar
          &two
            baz: qux
    postcss:
      addons:
        - name: nested
          url: https://github.com/postcss/postcss-nested
      code: |
        .extend-name {
          &-one {
            foo: bar;
          }
          &two {
            baz: qux;
          }
        }
    css:
      code: |
        .extend-name-one {
          foo: bar;
        }
        .extend-nametwo {
          baz: qux;
        }
  parent:
    description: "Allows to set a parent of the current nested selector `.ie7 &`"
    less:
      code: |
        .parent-selector {
          foo: bar;
          .ie7 & {
            baz: qux;
          }
        }
    scss:
      code: |
        .parent-selector {
          foo: bar;
          .ie7 & {
            baz: qux;
          }
        }
    sass:
      code: |
        .parent-selector
          foo: bar
          .ie7 &
            baz: qux
    stylus:
      code: |
        .parent-selector
          foo: bar
          .ie7 &
            baz: qux
    postcss:
      addons:
        - name: nested
          url: https://github.com/postcss/postcss-nested
      code: |
        .parent-selector {
          foo: bar;
          .ie7 & {
            baz: qux;
          }
        }
    css:
      code: |
        .parent-selector {
          foo: bar;
        }
        .ie7 .parent-selector {
          baz: qux;
        }
  prepend:
    description: "Prepend a selector, a nested `ol&` below `.test` returns `ol.test`"
    less:
      code: |
        .selector-prepend {
          foo: bar;
          ol& {
            baz: qux
          }
        }
    scss:
      issues: 
        - requires the `@at-root` directive
      version: 3.4.0
      code: |
        .selector-prepend {
          foo: bar;
          @at-root ol#{&} {
            baz: qux
          }
        }
    sass:
      issues: 
        - requires the `@at-root` directive
      version: 3.4.0
      code: |
        .selector-prepend
          foo: bar
          @at-root ol#{&}
            baz: qux
    stylus:
      code: |
        .selector-prepend
          foo: bar
          ol&
            baz: qux
    postcss:
      addons:
        - name: nested
          url: https://github.com/postcss/postcss-nested
      code: |
        .selector-prepend {
          foo: bar;
          ol& {
            baz: qux
          }
        }
    css:
      code: |
        .selector-prepend {
          foo: bar;
        }
        ol.selector-prepend {
          baz: qux;
        }
  multiple:
    description: "Ability to more than one parent reference in a selector `& + &`"
    less:
      code: |
        .multi-reference {
          foo: bar;
          & + & {
            baz: qux;
          }
        }
    scss:
      code: |
        .multi-reference {
          foo: bar;
          & + & {
            baz: qux;
          }
        }
    sass:
      code: |
        .multi-reference
          foo: bar
          & + &
            baz: qux
    stylus:
      code: |
        .multi-reference
          foo: bar
          & + &
            baz: qux
    postcss:
      addons:
        - name: nested
          url: https://github.com/postcss/postcss-nested
      code: |
        .multi-reference {
          foo: bar;
          & + & {
            baz: qux;
          }
        }
    css:
      code: |
        .multi-reference {
          foo: bar;
        }
        .multi-reference + .multi-reference {
          baz: qux;
        }
  explosion:
    description: "Generate all possible permutations of selectors from a list"
    less:
      code: |
        p, ul, ol {
          foo: bar;
          & + & {
            baz: qux;
          }
        }
    scss:
      issues:
        - requires additional variable
      code: |
        $selectors: "p, ul, ol";

        #{$selectors} {
          foo: bar;
          & + {
            #{$selectors} {
              baz: qux;
            }
          }
        }
    sass:
      issues:
        - requires additional variable
      code: |
        $selectors: "p, ul, ol"

        #{$selectors}
          foo: bar
          & +
            #{$selectors}
              baz: qux
    stylus:
      issues:
        - requires additional variable
      code: |
        $selectors = "p, ul, ol"

        {$selectors}
          foo: bar
          & +
            {$selectors}
              baz: qux
    css:
      code: |
        p,
        ul,
        ol {
          foo: bar;
        }
        p + p,
        p + ul,
        p + ol,
        ul + p,
        ul + ul,
        ul + ol,
        ol + p,
        ol + ul,
        ol + ol {
          baz: qux;
        }
  root:
    description: "Root directive/reference removes parent selectors for everything below it"
    scss:
      version: 3.3.0
      code: |
        .text {
          $anim-name: bounce;
          margin-top: 0;
          &:hover {
            animation: $anim-name 0.8s infinite ease-in alternate;
          }
          @at-root {
            @keyframes #{$anim-name} {
              50% {
                margin-top: -.5em;
              }
            }
          }
        }
    sass:
      version: 3.3.0
      code: |
        .text
          $anim-name: bounce
          margin-top: 0
          &:hover
            animation: $anim-name .8s infinite ease-in alternate
          @at-root
            @keyframes #{$anim-name}
              50%
                margin-top: -.5em
    stylus:
      version: 0.42.0
      code: |
        .text
          $anim-name = bounce
          margin-top: 0
          &:hover
            animation: $anim-name .8s infinite ease-in alternate
          /
            vendors = official
            @keyframes $anim-name
              50%
                margin-top: -.5em
    postcss:
      local: true
      addons:
        - name: simple-vars
          url: https://github.com/postcss/postcss-simple-vars
        - name: atroot
          url: https://github.com/OEvgeny/postcss-atroot
      code: |
        .text {
          $anim-name: bounce;
          margin-top: 0;
          &:hover {
            animation: $anim-name 0.8s infinite ease-in alternate;
          }
          @at-root {
            @keyframes $anim-name {
              50% {
                margin-top: -.5em;
              }
            }
          }
        }
    css:
      code: |
        .text {
          margin-top: 0;
        }
        .text:hover {
          animation: bounce 0.8s infinite ease-in alternate;
        }
        @keyframes bounce {
          50% {
            margin-top: -0.5em;
          }
        }

Placeholder Selectors:
  basic:
    description: "Reusable components that are outputted only if used somewhere, also known as silent classes"
    less:
      code: |
        .placeholder-1() {
          foo: bar;
        }

        .placeholder-2() {
          baz: qux;
        }

        .include-placeholder-1 {
          .placeholder-1();
        }
    scss:
      code: |
        %placeholder-1 {
          foo: bar;
        }

        %placeholder-2 {
          baz: qux;
        }

        .include-placeholder-1 {
          @extend %placeholder-1;
        }
    sass:
      code: |
        %placeholder-1
          foo: bar

        %placeholder-2
          baz: qux

        .include-placeholder-1
          @extend %placeholder-1
    stylus:
      code: |
        $placeholder-1
          foo: bar

        $placeholder-2
          baz: qux

        .include-placeholder-1
          @extend $placeholder-1
    postcss:
      local: true
      addons:
        - name: extend
          url: https://github.com/travco/postcss-extend
      code: |
        %placeholder-1 {
          foo: bar;
        }

        %placeholder-2 {
          baz: qux;
        }

        .include-placeholder-1 {
          @extend %placeholder-1;
        }
    css:
      code: |
        .include-placeholder-1 {
          foo: bar;
        }

Content Directive:
  basic:
    description: "Mixins accept style declarations inside it, useful if reusing selectors"
    less:
      code: |
        .active(@content) {
          &.is-active,
          &:hover {
            @content();
          }
        }

        .content-directive {
          foo: bar;
          .active({
            baz: qux
          });
        }
    scss:
      version: 3.2.0
      code: |
        @mixin active {
          &.is-active,
          &:hover {
            @content;
          }
        }

        .content-directive {
          foo: bar;
          @include active {
            baz: qux;
          }
        }
    sass:
      version: 3.2.0
      code: |
        =active()
          &.is-active,
          &:hover
            @content

        .content-directive
          foo: bar
          +active()
            baz: qux
    stylus:
      version: 0.41.0
      code: |
        active()
          &.is-active,
          &:hover
            {block}

        .content-directive
          foo: bar
          +active()
            baz: qux
    postcss:
      local: true
      addons:
        - name: mixins
          url: https://github.com/postcss/postcss-mixins
        - name: nested
          url: https://github.com/postcss/postcss-nested
      code: |
        @define-mixin active {
          &.is-active,
          &:hover {
            @mixin-content;
          }
        }

        .content-directive {
          foo: bar;
          @mixin active {
            baz: qux;
          }
        }
    css:
      code: |
        .content-directive {
          foo: bar;
        }
        .content-directive.is-active,
        .content-directive:hover {
          baz: qux;
        }
  media:
    description: "Mixins accept both style declarations and parameters, useful for simpler media queries"
    less:
      code: |
        .media-max(@num, @content) {
          @media only screen and (max-width: @num) {
            @content();
          }
        }

        .media-test {
          width: 400px;
          .media-max(980px, {
            max-width: 100%;
          });
        }
    scss:
      version: 3.2.0
      code: |
        @mixin media-max($num) {
          @media only screen and (max-width: $num) {
            @content;
          }
        }

        .media-test {
          width: 400px;
          @include media-max(980px) {
            max-width: 100%;
          }
        }
    sass:
      version: 3.2.0
      code: |
        =media-max($num)
          @media only screen and (max-width: $num)
            @content

        .media-test
          width: 400px
          +media-max(980px)
            max-width: 100%
    stylus:
      version: 0.41.0
      code: |
        media-max($num)
          @media only screen and (max-width: $num)
            {block}

        .media-test
          width: 400px
          +media-max(980px)
            max-width: 100%
    postcss:
      local: true
      addons:
        - name: mixins
          url: https://github.com/postcss/postcss-mixins
      code: |
        @define-mixin media-max $num {
          @media only screen and (max-width: $num) {
            @mixin-content;
          }
        }
        .media-test {
          width: 400px;
          @mixin media-max 980px {
            max-width: 100%;
          }
        }
    css:
      code: |
        .media-test {
          width: 400px;
        }
        @media only screen and (max-width: 980px) {
          .media-test {
            max-width: 100%;
          }
        }
  placeholder text:
    description: "Ability to reuse the content block in several selectors, useful for placeholder-text mixin"
    less:
      code: |
        .placeholder-text(@content) {
          &::-webkit-input-placeholder {
            @content();
          }
          &::-moz-placeholder {
            @content();
          }
          &:-ms-input-placeholder {
            @content();
          }
        }

        .text-input {
          .placeholder-text({
            color: #acacac;
            text-transform: uppercase;
          });
        }
    scss:
      version: 3.2.0
      code: |
        @mixin placeholder-text {
          &::-webkit-input-placeholder {
            @content;
          }
          &::-moz-placeholder {
            @content;
          }
          &:-ms-input-placeholder {
            @content;
          }
        }

        .text-input {
          @include placeholder-text {
            color: #acacac;
            text-transform: uppercase;
          }
        }
    sass:
      version: 3.2.0
      code: |
        =placeholder-text()
          &::-webkit-input-placeholder
            @content

          &::-moz-placeholder
            @content

          &:-ms-input-placeholder
            @content

        .text-input
          +placeholder-text()
            color: #acacac
            text-transform: uppercase
    stylus:
      version: 0.41.0
      code: |
        placeholder-text()
          &::-webkit-input-placeholder
            {block}

          &::-moz-placeholder
            {block}

          &:-ms-input-placeholder
            {block}

        .text-input
          +placeholder-text()
            color: #acacac
            text-transform: uppercase
    postcss:
      local: true
      addons:
        - name: mixins
          url: https://github.com/postcss/postcss-mixins
      code: |
        @define-mixin placeholder-text {
          &::-webkit-input-placeholder {
            @mixin-content;
          }
          &::-moz-placeholder {
            @mixin-content;
          }
          &:-ms-input-placeholder {
            @mixin-content;
          }
        }

        .text-input {
          @mixin placeholder-text {
            color: #acacac;
            text-transform: uppercase;
          }
        }
    css:
      code: |
        .text-input::-webkit-input-placeholder {
          color: #acacac;
          text-transform: uppercase;
        }
        .text-input::-moz-placeholder {
          color: #acacac;
          text-transform: uppercase;
        }
        .text-input:-ms-input-placeholder {
          color: #acacac;
          text-transform: uppercase;
        }

Loops:
  basic:
    description: "Loops that can increment values"
    less:
      issues:
        - need to call the function
      code: |
        .generate-column(@i: 1) when (@i =< 4) {
          .col-@{i} {
            width: @i * (100% / 4);
          }
          .generate-column(@i + 1);
        }

        .generate-column();
    less-alt:
      issues:
        - looks very hacky
      code: |
        .col {
          .-(@i: 1) when (@i =< 4) {
            &-@{i} {
              width: @i * (100% / 4);
            }.-(@i + 1);
          }.-;
        }
    scss:
      code: |
        @for $i from 1 through 4 {
          .col-#{$i} {
            width: $i * (100% / 4);
          }
        }
    sass:
      code: |
        @for $i from 1 through 4
          .col-#{$i}
            width: $i * (100% / 4)
    stylus:
      code: |
        for $i in 1..4
          .col-{$i}
            width: $i * (100% / 4)
    postcss:
      local: true
      addons:
        - name: for
          url: https://github.com/antyakushev/postcss-for
        - name: mathjs
          url: https://github.com/dtrussia/postcss-mathjs
      code: |
        @for $i from 1 to 4 {
          .col-$(i) {
            width: resolve($(i) * resolve(100 / 4))%;
          }
        }
    css:
      code: |
        .col-1 {
          width: 25%;
        }
        .col-2 {
          width: 50%;
        }
        .col-3 {
          width: 75%;
        }
        .col-4 {
          width: 100%;
        }
  intermediate:
    description: "Loops that iterate though items in a list"
    less:
      issues:
        - need to call the function
      code: |
        @colors: #012345, #6789ab, #abcdef;

        .color-mixin(@colors; @n) when (@n > 0) {
          .color-mixin(@colors; (@n - 1));
          @i: @n - 1;
          .color-@{i} {
            background-color: extract(@colors, @n);
          }
        }
        .color-mixin(@colors; length(@colors));
    scss:
      code: |
        $colors: #012345, #6789ab, #abcdef;

        @for $i from 1 through length($colors) {
          .color-#{$i - 1} {
            background: #{nth($colors, $i)};
          }
        }
    scss-alt:
      code: |
        $colors: #012345, #6789ab, #abcdef;

        @each $color in $colors {
          $i: index($colors, $color);
          .color-#{$i - 1} {
            background: $color;
          }
        }
    sass:
      code: |
        $colors: #012345, #6789ab, #abcdef

        @for $i from 1 through length($colors)
          .color-#{$i - 1}
            background: #{nth($colors, $i)}
    sass-alt:
      code: |
        $colors: #012345, #6789ab, #abcdef

        @each $color in $colors
          $i: index($colors, $color)
          .color-#{$i - 1}
            background: $color
    stylus:
      code: |
        $colors = #012345, #6789ab, #abcdef

        for $color, $i in $colors
          .color-{$i}
            background: $color
    postcss:
      addons:
        - name: each
          url: https://github.com/outpunk/postcss-each
      code: |
        $colors = #012345, #6789ab, #abcdef;
        @each $color, $i in ($colors) {
          .color-$(i) {
            background: $(color);
          }
        }
    css:
      code: |
        .color-0 {
          background: #012345;
        }
        .color-1 {
          background: #6789ab;
        }
        .color-2 {
          background: #abcdef;
        }

Image Helpers:
  dimensions:
    description: "Ability to get the width/height of a given image"
    less:
      local: true
      version: 2.2.0
      code: |
        .image {
          bg-size: image-size("img.png");
          bg-width: image-width("img.png");
          bg-height: image-height("img.png");
        }
    scss:
      local: true
      addons:
        - name: compass
          url: http://compass-style.org/
      code: |
        .image {
          bg-size: image-width("img.png") image-height("img.png");
          bg-width: image-width("img.png");
          bg-height: image-height("img.png");
        }
    sass:
      local: true
      addons:
        - name: compass
          url: http://compass-style.org/
      code: |
        .image
          bg-size: image-width("img.png") image-height("img.png")
          bg-width: image-width("img.png")
          bg-height: image-height("img.png")
    stylus:
      local: true
      code: |
        .image
          bg-size: image-size("img.png")
          bg-width: image-size("img.png")[0]
          bg-height: image-size("img.png")[1]
    postcss:
      local: true
      addons:
        - name: assets
          url: https://github.com/assetsjs/postcss-assets
      code: |
        .image {
          width: width("img.png");
          height: height("img.png");
          background-size: size("img.png");
        }
    css:
      code: |
        .image {
          bg-size: 20px 10px;
          bg-width: 20px;
          bg-height: 10px;
        }
  inline:
    description: "Encode a given image to base64"
    less:
      local: true
      version: 1.4.0
      code: |
        .image {
          background-image: data-uri("pixel.png");
        }
    scss:
      local: true
      addons:
        - name: compass
          url: http://compass-style.org/
      code: |
        .image {
          background-image: inline-image("pixel.png");
        }
    sass:
      local: true
      addons:
        - name: compass
          url: http://compass-style.org/
      code: |
        .image
          background-image: inline-image("pixel.png")
    stylus:
      local: true
      issues:
        - requires function definition
      code: |
        .image
          background-image: inline-image("pixel.png")
    postcss:
      local: true
      addons:
        - name: assets
          url: https://github.com/assetsjs/postcss-assets
      code: |
        .image {
          background-image: inline("pixel.png");
        }
    css:
      code: |
        .image {
          background-image: url("data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7")
        }

Sourcemaps:
  basic:
    description: "Generate a source map file that defines a mapping between CSS declarations and the corresponding line of the source file"
    less:
      version: 1.5.0
    scss:
      version: 3.3.0
    sass:
      version: 3.3.0
    postcss:
      code: |
    stylus:
      version: 0.48.0

Color Extract:
  RGB:
    description: "Extract the Red, Green, or Blue components from a color"
    less:
      code: |
        .rgb-components {
          red-component-rgb: red(rgb(81, 104, 148));
          red-component-hsl: red(hsl(220, 29%, 45%));
          red-component-hex: red(#516894);
          green-component-rgb: green(rgb(81, 104, 148));
          green-component-hsl: green(hsl(220, 29%, 45%));
          green-component-hex: green(#516894);
          blue-component-rgb: blue(rgb(81, 104, 148));
          blue-component-hsl: blue(hsl(220, 29%, 45%));
          blue-component-hex: blue(#516894);
        }
    scss:
      code: |
        .rgb-components {
          red-component-rgb: red(rgb(81, 104, 148));
          red-component-hsl: red(hsl(220, 29%, 45%));
          red-component-hex: red(#516894);
          green-component-rgb: green(rgb(81, 104, 148));
          green-component-hsl: green(hsl(220, 29%, 45%));
          green-component-hex: green(#516894);
          blue-component-rgb: blue(rgb(81, 104, 148));
          blue-component-hsl: blue(hsl(220, 29%, 45%));
          blue-component-hex: blue(#516894);
        }
    sass:
      code: |
        .rgb-components
          red-component-rgb: red(rgb(81, 104, 148))
          red-component-hsl: red(hsl(220, 29%, 45%))
          red-component-hex: red(#516894)
          green-component-rgb: green(rgb(81, 104, 148))
          green-component-hsl: green(hsl(220, 29%, 45%))
          green-component-hex: green(#516894)
          blue-component-rgb: blue(rgb(81, 104, 148))
          blue-component-hsl: blue(hsl(220, 29%, 45%))
          blue-component-hex: blue(#516894)
    stylus:
      code: |
        .rgb-components
          red-component-rgb: red(rgb(81, 104, 148))
          red-component-hsl: red(hsl(220, 29%, 45%))
          red-component-hex: red(#516894)
          green-component-rgb: green(rgb(81, 104, 148))
          green-component-hsl: green(hsl(220, 29%, 45%))
          green-component-hex: green(#516894)
          blue-component-rgb: blue(rgb(81, 104, 148))
          blue-component-hsl: blue(hsl(220, 29%, 45%))
          blue-component-hex: blue(#516894)
    css:
      code: |
        .rgb-components {
          red-component-rgb: 81;
          red-component-hsl: 81;
          red-component-hex: 81;
          green-component-rgb: 104;
          green-component-hsl: 104;
          green-component-hex: 104;
          blue-component-rgb: 148;
          blue-component-hsl: 148;
          blue-component-hex: 148;
        }
  HSL:
    description: "Extract the Hue, Saturation, or Lightness components from a color"
    less:
      code:
    scss:
      code:
    sass:
      code:
    stylus:
      code:
  HSV:
    description: "Extract the Hue, Saturation, or Value/Brightness components from a color"
    less:
      code:
    sass:
      issues:
        - requires custom function
    stylus:
      issues:
        - requires custom function
  alpha:
    description: "Extract the value of the alpha channel from a color"
    less:
      code:
    scss:
      code:
    sass:
      code:
    stylus:
      code:
  luma/luminosity:
    less:
      code:
    stylus:
      version: 0.47.0
      code:
  luminance:
    less:
      code:
  contrast:
    stylus:
      version: 0.47.0
      code:

Color Test:
  dark:
    description: "Returns true if the color is dark"
    less:
      issues:
        - requires custom function
    scss:
      issues:
        - requires custom function
    sass:
      issues:
        - requires custom function
    stylus:
      code:
  light:
    description: "Returns true if the color is light"
    less:
      issues:
        - requires custom function
    scss:
      issues:
        - requires custom function
    sass:
      issues:
        - requires custom function
    stylus:
      code:
